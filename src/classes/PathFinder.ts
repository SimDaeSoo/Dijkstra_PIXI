import { Tile } from "../interfaces/Tile";

export default class PathFinder {
    public static dijkstra(tiles: Array<Tile>, from: Tile, to: Tile): Array<Tile> {
        const path: Array<Tile> = [];

        return path;
    }
}

